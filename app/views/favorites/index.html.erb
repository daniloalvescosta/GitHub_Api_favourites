
<%= render 'home/search_form' %>
<% @favorites.each do |f| %>
    <%= f.name %>  <br>
    <%= f.owner %>  <br>
    <%= "#{f.created_at} 
    | #{f.updated_at}" %> <br>
    <%= link_to "Show Repository", favorite_path(f), method: :show %> <br>
    <br>
<% end %>
